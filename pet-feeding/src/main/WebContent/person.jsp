<!DOCTYPE html>
<html>
<head>
</head>
    <body>
    <table>
        <!--<c:forEach items="${persons}" var="person">
            <tr>
                <td>${person.id}</td>
                <td>${person.name}</td>
                <td>${person.email}</td>
                <td>${person.password}</td>
            </tr>
        </c:forEach>-->
    </table>
    </body>
</html>
